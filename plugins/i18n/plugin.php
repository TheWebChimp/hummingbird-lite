<?php
	class I18N {
		// protected $locales;
		// protected $locale;

		// function __construct($settings) {
		// 	$this->locales = array();
		// 	$locale = '';
		// }

		// function addLocale($key, $translation) {
		// 	// $strings =
		// 	// $this->locales[$key] = $strings;
		// }

		// function setLocale($key) {
		// 	$this->locale = $key;
		// }

		// function t($key) {
		// 	$ret = $key;
		// 	if (! empty($locale) && isset( $this->locales[$locale][$key] ) ) {
		// 		$ret = $this->locales[$locale][$key];
		// 	}
		// 	return $ret;
		// }
	}

	$i18n = new I18N();
?>